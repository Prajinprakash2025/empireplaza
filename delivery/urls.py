# from django.urls import path
# from .views import AvailableDeliveriesView, AcceptDeliveryView, CompleteDeliveryView

# urlpatterns = [
#     path('available/', AvailableDeliveriesView.as_view(), name='available_deliveries'),
#     path('accept/<int:order_id>/', AcceptDeliveryView.as_view(), name='accept_delivery'),
#     path('complete/<int:delivery_id>/', CompleteDeliveryView.as_view(), name='complete_delivery'),
# ]